from datetime import datetime

def get_days_from_today(date):
    difference = datetime.today() - datetime.strptime(date, "%Y-%m-%d")
    return difference.days

print(get_days_from_today('2024-09-02'))
